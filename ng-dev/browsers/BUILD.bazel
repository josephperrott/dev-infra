package(default_visibility = ["//visibility:public"])

# Make source files available for distribution via pkg_npm
filegroup(
    name = "files",
    srcs = glob(["*"]) + [
        "//ng-dev/browsers/chromium:files",
        "//ng-dev/browsers/firefox:files",
    ],
)
